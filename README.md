# OOP_Labs

Лабораторні роботи з ООП, КПІ ім. Ігоря Сікорського

Степаненко Денис, ІМ-051, 121 «Інженерія програмного забезпечення»

Викладач: Рекечинський Дмитро Олександрович

## Лабораторні

Всі лаби на C++ (Win32 API). Кожна наступна лаба продовжує попередню — в результаті виходить графічний редактор об'єктів.

| # | Лаба | Тема | Статус |
|---|------|------|--------|
| 1 | lab1 | Win32, модульний проєкт, діалогові вікна з окремих модулів | ✅ Готова |
| 2 | lab2 | Ієрархія класів Shape, поліморфізм, малювання фігур мишею | ✅ Готова |
| 3 | lab3 | Toolbar, tooltips, обробники як методи класу, діаграми UML | ✅ Готова |
| 4 | lab4 | Рефакторинг, множинне успадкування, клас MyEditor | 🔄 В процесі |
| 5 | lab5 | Багатовіконний інтерфейс, Singleton, таблиця об'єктів, запис у файл | ⬜ Не почата |
| 6 | lab6 | Три незалежні програми, обмін через WM_COPYDATA та Clipboard | ⬜ Не почата |

**Легенда:** ⬜ Не почата | 🔄 В процесі | ✅ Готова

В кожній папці `labN/` є `task.md` — умова завдання.

## Технології

C++26, Win32 API, MinGW (LLVM-MinGW g++)

## Збірка

Кожна лаба збирається MinGW g++ у три кроки:

1. `g++ -c file.cpp -o file.o` — компіляція кожного .cpp в об'єктний файл
2. `windres -c 65001 file.rc -o file.res.o` — компіляція ресурсів (меню, діалоги)
3. `g++ -municode -mwindows -o Lab.exe *.o *.res.o -lgdi32 -luser32 -lcomctl32` — лінкування в exe

Прапорці: `-municode` — вхід через wWinMain (Unicode); `-mwindows` — GUI-застосунок
без консолі; `-l*` — системні бібліотеки Win32; `-std=c++26` — стандарт мови.

```powershell
# Зібрати всі лаби одразу (з кореня репо):
.\build.bat

# Або одну лабу (автоматично знаходить і компілює всі .cpp/.rc):
.\build-lab.bat 3        # тільки збірка
.\build-lab.bat 3 run    # збірка + запуск
```

## Запуск

```powershell
cd labs\labN\code\src
.\LabN.exe
```

## Структура

```
labs/labN/        — task.md + code/src/*.cpp + report/ + screenshots/
oop_docs/         — силабус, методичка
build.bat         — збірка всіх лаб
build-lab.bat N   — збірка однієї лаби (авто-пошук .cpp/.rc)
```
